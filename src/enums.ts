export enum containerTypeEnum {
    bubble = "bubble",
    carousel = "carousel"
}

export enum containerSizeEnum {
    nano = "nano",
    micro = "micro",
    kilo = "kilo",
    mega = "mega",
    giga = "giga"
}
export enum containerDirection {
    ltr = "ltr",
    rtl = "rtl",
}

export enum blockTypeEnum {
    box = "box"
}

export enum blockLayoutEnum {
    horizontal = "horizontal",
    vertical = "vertical",
    baseline = "baseline"
}